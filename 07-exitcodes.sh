#!/bin/bash

# Each and every action in the linux will have the exit codes
# The Range of Exit codes are from 0 to 255
# Among all '0' represents action completed successfull
# Anything in between 1 to 255 represet either partial success, partial failure, failure exit codes

